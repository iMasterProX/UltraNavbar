<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/imgWidgetBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="false"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_background" />

    <!-- Main content (centered) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">

        <!-- Header: keyboard icon + device name -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/imgKeyboard"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_widget_keyboard"
                android:tint="@color/widget_icon_tint"
                android:contentDescription="Keyboard" />

            <TextView
                android:id="@+id/txtKeyboardName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Keyboard"
                android:textSize="13sp"
                android:textColor="@color/widget_text_secondary"
                android:ellipsize="end"
                android:maxLines="1" />
        </LinearLayout>

        <!-- Battery percentage (large) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical|center_horizontal"
            android:layout_marginTop="6dp">

            <ImageView
                android:id="@+id/imgBattery"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/ic_widget_battery"
                android:contentDescription="Battery" />

            <TextView
                android:id="@+id/txtBatteryLevel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="85%"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="#4CAF50" />
        </LinearLayout>

        <!-- Battery progress bar -->
        <ProgressBar
            android:id="@+id/batteryBar"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="10dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:max="100"
            android:progress="85"
            android:progressDrawable="@drawable/widget_battery_progress"
            android:background="@drawable/widget_battery_bar_bg" />

        <!-- Last updated -->
        <TextView
            android:id="@+id/txtLastUpdated"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Updated: 10:30"
            android:textSize="11sp"
            android:textColor="@color/widget_text_tertiary" />
    </LinearLayout>

    <!-- Refresh button (bottom-right corner) -->
    <ImageView
        android:id="@+id/btnRefresh"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="14dp"
        android:src="@drawable/ic_widget_refresh"
        android:tint="@color/widget_icon_tint"
        android:contentDescription="Refresh"
        android:padding="2dp" />

</FrameLayout>
