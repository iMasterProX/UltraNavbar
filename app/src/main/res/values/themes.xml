<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.UltraNavbar" parent="@style/Theme.Material3.DayNight">
        <!-- Manually enable dynamic color since the DynamicColors parent theme is failing to resolve -->
    </style>

    <style name="Theme.UltraNavbar.Fullscreen" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.UltraNavbar.Transparent" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>